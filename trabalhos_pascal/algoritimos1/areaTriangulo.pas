Program areaTriangulo ;
var
	base, altura : real;
Begin
    writeln('digite o tamanho da base do triangulo:');
    readln(base);
    writeln('digite o tamanho da altura do triangulo:');
    readln(altura);
    writeln('a área do triangulo é: ', (base*altura)/2);
End.